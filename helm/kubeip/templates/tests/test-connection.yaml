apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubeip.fullname" . }}-test-connection"
  labels:
    {{- include "kubeip.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubeip.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
